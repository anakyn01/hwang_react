(1)
create table 학생(
학번 int, 이름 varchar(20),
학년 int, 전공 varchar(30),
전화번호varchar(20)
);

insert into 학생(학번,이름,학년,전공,전화번호)
values(9830287,'뉴진스',3,'경영학개론','010-1234-1234');

학생 테이블을 참조하는 다른 VIEW나 제약조건까지 모두 삭제되어야 한다
DROP VIEW 학생 cascade

테스트 커버리지
[조건 커버리지]
- 프로그램 내에 있는 결정포인트 내의 모든 각 개별 
조건식에 대한 모든 가능한 결과
(참/거짓)에 대해 적어도 한번 수행한다.
- 소프트웨어 테스트 수행시 소스코드를 어느 수준까지 
수행하였는가를 나타내는 기준을 나타낸다.
- 실제 업무에서는 다양한 툴을 사용하여 테스팅 수행한다.
- True/False 에충분한 영향을 줄 수 없는 경우가 
발생 가능한 한계점을 지닌다.

[템퍼프루핑]
- 소프트웨어에서 워터마크가 삭제등과 같이 불법으로변경
정상적으로 수행이 안됨
- 변조검증코드(tamper-proofing code)를 삽입하는것도 한방법
- 프로그램이 변경되었는지 탐지 둘째 변조가 탐지됨녀
프로그램이 실행되지 않게 합니다
- 소프트웨어의 위변조 방지 여공학 기술의 일종
- 디지털콘텐츠 관련산업이나
- 전자상거레 또는 보호해야될 소프트웨어

디자인 패턴에 대한 문제
[Singleton]
생성자가 여러 차례 호출되더라도 생성되는 객체는 하나이고 최초생성 이후에
최초의 생성자가 생성한 객체를 리턴한다
주로 공통된 개체를 여러개 생성해서 사용하는 DataBase Connection Pool
와 같은 상황에서 많이 사용합니다

[Visitor]
호스트 객체의 내부 상태에 접근할 수 있는 방법을 제공하여 
호스트 객체에 연산을 추가할 수 있도록 한다.
이 패턴은 보통 합성 구조의 원소들과 상호 작용하는 데 사용되며, 
기존 코드를 변경하지 않고 새로운 기능을 추가하는 방법이다.

( 1  Hamming) Code는  데이터 전송시 1 비트의 에러를 정정할 수 있는, 
오류정정부호의 일종으로 미국의 Bell 연구소의 Hamming에 의해 고안되었다. 
선형블록부호 및 순회부호에 속에 속한다.

( 2 FEC) 은/는 송신측이 전송할 문자나 프레임에 부가적 정보(Redundancy)를 
첨가하여 전송하고 수신측이 이 부가적 정보를 이용하여 에러검출 및 에러정정을 하는 방식이다.

 ( 3 BEC ) 은/는 데이터 전송 과정에서 오류가 발생하면 송신 측에 
 재전송을 요구하는 방식이다. 오류를 검출하는 방법은 Parity검사와 CRC,
  블록 합 검사 등이 있다.

 

( 4 parity) 은/는 데이터가 저장장치 내의 한 장소에서 다른 장소로 이동되거나, 
컴퓨터들간에 전송될 때, 데이터가 유실 또는 손상되었는지 여부를 점검하는
 기술과 관련된 용어이다.

( 5 CRC) 은/는 네트워크 등을 통하여 데이터를 전송할 때 
전송된 데이터에 오류가 있는지를 확인하기 위한 체크값을 결정하는 방식을 말한다.

데이터베이스에 설계순서를 작성하세요

구현, 요구조건 분석, 개념적 설계, 물리적 설계, 논리적 설계

(1) 요구조건 분석         
(2) 개념적 설계
(3) 논리적 설계
(4) 물리적 설계
(5) 구현

암호와 알고리즘에 대한 내용이다

대칭키 (DES, AES, ARIA, SEED)
바대칭키(RSA, ECC)

