#include <stdio.h>

//어떤 숫자(number)가 소수인지 판별
int isPrime(int number){
    int i;
    for (i=2; i<number; i++){
/*
2부터 시작해서 입력받은 숫자보다 
1 작은 수(number-1)까지 1씩 증가하며 반복합니다
1과 자기자신을 제외한 숫자로 나누어 보기 위함입니다
*/    
if(number % i == 0) return 0;
/*
만약 number가 i로 나누어 떨어진다면(나머지가 0), 
약수가 존재한다는 뜻이므로 소수가 아닙니다.
따라서 0(거짓)을 반환하고 즉시 함수를 종료합니다.
*/    
    }
    //소수일경우 참을 반환
    return 1;
}

int main(){
    int number = 13195, max_div=0, i;

    for (i=2; i<number; i++){
        if(isPrime(i) == 1 && number % i == 0) max_div = i;
        /*
        두 가지 조건을 AND(&&)로 동시에 만족하는지 확인합니다.
        1. isPrime(i) == 1 : 현재 숫자 i가 소수인가?
        2. number % i == 0 : 현재 숫자 i가 13195를 
        나누어 떨어지게 하는 수(약수)인가?
        둘 다 만족한다면(즉, 소인수라면) max_div에 그 숫자 i를 저장
        숫자가 커지면서 계속 덮어씌워지므로, 
        최종적으로 가장 큰 소인수가 남습니다.
        */
      
       //
    }
     printf("%d", max_div);
     return 0;
}

/*
어진 숫자(13195)의 가장 큰 소인수
(Largest Prime Factor)를 구하는 알고리즘입니다.

이코드에는 두개의 핵심동작이 있습니다
isPrime 함수 (소수 판독기) :
어떤 숫자를 넣었을 때, 
그 수가 소수(1과 자기 자신만으로 나누어 떨어지는 수)인지 
검사합니다
2부터 자기 자신보다 1 작은 수까지 전부 나누어 보면서,
 한 번이라도 나누어 떨어지면 소수가 아니라고 
 판단(0 반환)하고, 다 통과하면 소수라고 판단(1 반환)합니다.

 main 반복문 (소인수 찾기):
 숫자 2부터 13194까지 순서대로 검사하면서
 그 숫자가 ①
 소수이면서 동시에 
 ② 13195의 약수(나누어 떨어지는 수)인지 확인합니다.
 조건을 만족할 때마다 max_div라는 변수에 
 저장(덮어쓰기)하므로, 반복문이 끝났을 때는 
 자연스럽게 가장 마지막에 발견된 가장 큰 소인수가 남게 됩니다.
*/